<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Full-Stack App (Student Build)</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    /* Basic Page Handling */
    .page { display: none; }
    .page.active { display: block; }

    /* Auth Visibility Toggles */
    body.not-authenticated .role-logged-in { display: none; }
    body.authenticated .role-logged-out { display: none; }

    /* Admin Visibility Toggles */
    .role-admin { display: none; }
    body.is-admin .role-admin { display: block; }
    
    /* Smooth transition for navbar */
    .navbar-brand { font-weight: bold; }
  </style>
</head>

<body class="not-authenticated">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="#/">Full-Stack App</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">

        <li class="nav-item role-logged-out">
          <a class="nav-link" href="#/login">Login</a>
        </li>
        <li class="nav-item role-logged-out">
          <a class="nav-link" href="#/register">Register</a>
        </li>

        <li class="nav-item dropdown role-logged-in">
          <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
            <span id="nav-username">User</span>
          </a>

          <ul class="dropdown-menu dropdown-menu-end">
            <li><a class="dropdown-item" href="#/profile">Profile</a></li>
            <li><a class="dropdown-item" href="#/requests">My Requests</a></li>

            <li class="role-admin"><hr class="dropdown-divider"></li>
            <li class="role-admin"><h6 class="dropdown-header">Admin Console</h6></li>
            <li class="role-admin"><a class="dropdown-item" href="#/employees">Employees</a></li>
            <li class="role-admin"><a class="dropdown-item" href="#/accounts">Accounts</a></li>
            <li class="role-admin"><a class="dropdown-item" href="#/departments">Departments</a></li>

            <li><hr class="dropdown-divider"></li>
            <li>
              <a class="dropdown-item text-danger" href="#" id="logoutBtn">Logout</a>
            </li>
          </ul>
        </li>

      </ul>
    </div>
  </div>
</nav>

<main class="container my-5">

  <section id="home-page" class="page active">
    <div class="p-5 mb-4 bg-light rounded-3 shadow-sm">
      <h1 class="display-5 fw-bold">Welcome to Full-Stack App</h1>
      <p class="col-md-8 fs-4">A static prototype before backend integration. Simulates:</p>
      <ul>
        <li>Email registration + fake verification</li>
        <li>Login with JWT-like token simulation</li>
        <li>Role-based UI (Admin/User)</li>
        <li>CRUD for Employees, Departments, Requests</li>
      </ul>
      <div class="alert alert-info">
        <strong>⚠️ Note:</strong> All data is stored in <code>localStorage</code>. Refreshing keeps data, but clearing cache resets it.
      </div>
      <a href="#/register" class="btn btn-primary btn-lg role-logged-out">Get Started &rarr;</a>
    </div>
  </section>

  <section id="register-page" class="page">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card shadow">
          <div class="card-header bg-primary text-white">Register</div>
          <div class="card-body">
            <form id="registerForm">
              <div class="mb-2">
                <label>First Name</label>
                <input class="form-control" placeholder="First Name" required id="regFirst">
              </div>
              <div class="mb-2">
                 <label>Last Name</label>
                <input class="form-control" placeholder="Last Name" required id="regLast">
              </div>
              <div class="mb-2">
                 <label>Email</label>
                <input class="form-control" type="email" placeholder="Email" required id="regEmail">
              </div>
              <div class="mb-2">
                 <label>Password</label>
                <input class="form-control" type="password" placeholder="Min 6 chars" required id="regPass">
              </div>
              
              <div class="mb-3">
                <label>Role (Simulation Only)</label>
                <select class="form-select" id="regRole" required>
                  <option value="user" selected>User</option>
                  <option value="admin">Admin</option>
                </select>
              </div>

              <button class="btn btn-primary w-100">Register</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="verify-email-page" class="page text-center">
    <h2>Email Verification</h2>
    <p class="lead" id="verifyMsg"></p>
    <button class="btn btn-success btn-lg" id="verifyBtn">✅ Simulate Clicking Email Link</button>
  </section>

  <section id="login-page" class="page">
    <div class="row justify-content-center">
      <div class="col-md-5">
        <div class="card shadow">
          <div class="card-header bg-dark text-white">Login</div>
          <div class="card-body">
            <form id="loginForm">
              <div class="mb-3">
                <label>Email</label>
                <input class="form-control" type="email" placeholder="admin@example.com" required id="loginEmail">
              </div>
              <div class="mb-3">
                <label>Password</label>
                <input class="form-control" type="password" placeholder="Password123!" required id="loginPass">
              </div>
              <button class="btn btn-primary w-100">Login</button>
            </form>
            <div class="mt-3 text-center">
               <small>Default Admin: admin@example.com / Password123!</small>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="profile-page" class="page">
    <h2>My Profile</h2>
    <div class="card">
        <div class="card-body" id="profileInfo">
            </div>
    </div>
  </section>

  <section id="accounts-page" class="page">
    <h2>All Registered Accounts</h2>
    <table class="table table-striped table-bordered mt-3">
        <thead class="table-dark">
            <tr><th>Name</th><th>Email</th><th>Role</th><th>Verified</th></tr>
        </thead>
        <tbody id="accountsTable"></tbody>
    </table>
  </section>

  <section id="departments-page" class="page">
    <h2>Departments</h2>
    <ul id="deptList" class="list-group mt-3"></ul>
  </section>

  <section id="employees-page" class="page">
    <h2>Employees Directory</h2>
    <p class="text-muted">Currently empty (Seed data simulation)</p>
  </section>

  <section id="requests-page" class="page">
    <h2>My Requests</h2>
    <div class="alert alert-secondary">No active requests found.</div>
    <table class="table" id="requestsTable"></table>
  </section>

</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

<script>
/* =======================
   STORAGE & STATE
======================= */
const STORAGE_KEY = 'ipt_demo_v1';
let currentUser = null;
window.db = { accounts: [], departments: [], requests: [] };

function loadFromStorage() {
  try {
    const data = JSON.parse(localStorage.getItem(STORAGE_KEY));
    if (data) window.db = data;
    else seed();
  } catch { seed(); }
}

function seed() {
  window.db = {
    accounts: [{
      firstName: 'System',
      lastName: 'Admin',
      email: 'admin@gmail.com',
      password: '123456',
      role: 'admin',
      verified: true
    }],
    departments: [
      { id: 1, name: 'Engineering' },
      { id: 2, name: 'Human Resources' },
      { id: 3, name: 'Finance' }

    ],
    requests: []
  };
  saveToStorage();
}

function saveToStorage() {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(window.db));
}

/* =======================
   ROUTER
======================= */
function navigateTo(hash) {
  location.hash = hash;
}

function handleRouting() {
  let hash = location.hash || '#/';
  // Hide all pages
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));

  // Route Guards
  const protectedRoutes = ['#/profile', '#/requests'];
  const adminRoutes = ['#/accounts', '#/departments', '#/employees'];

  if (!currentUser && protectedRoutes.includes(hash)) {
    return navigateTo('#/login');
  }

  if (currentUser?.role !== 'admin' && adminRoutes.includes(hash)) {
    alert("Access Denied: Admins Only");
    return navigateTo('#/');
  }

  // Determine Page ID
  const pageId = (hash.replace('#/', '') || 'home') + '-page';
  const pageEl = document.getElementById(pageId);
  
  if (pageEl) {
      pageEl.classList.add('active');
  } else {
      // 404 fallback to home
      document.getElementById('home-page').classList.add('active');
  }

  // Render Data
  if (hash === '#/profile') renderProfile();
  if (hash === '#/accounts') renderAccounts();
  if (hash === '#/departments') renderDepartments();
  if (hash === '#/requests') renderRequests();

  if (hash === '#/verify-email') {
    const email = localStorage.getItem('unverified_email');
    verifyMsg.textContent = email 
      ? `Verification link sent to ${email}` 
      : 'No email pending verification.';
  }

  if (hash === '#/register') {
    registerForm.reset();
  }
}

/* =======================
   AUTH LOGIC
======================= */
function setAuthState(isAuth, user = null) {
  currentUser = user;
  document.body.classList.toggle('authenticated', isAuth);
  document.body.classList.toggle('not-authenticated', !isAuth);
  document.body.classList.toggle('is-admin', user?.role === 'admin');

  if (user) {
    document.getElementById('nav-username').textContent = user.firstName;
  }
}

/* =======================
   FORM HANDLERS
======================= */
// Register
registerForm.onsubmit = e => {
  e.preventDefault();
  const email = regEmail.value;
  
  // Check duplicates
  if (db.accounts.find(a => a.email === email)) return alert('Email already registered');

  db.accounts.push({
    firstName: regFirst.value,
    lastName: regLast.value,
    email,
    password: regPass.value,
    role: regRole.value, 
    verified: false
  });

  localStorage.setItem('unverified_email', email);
  saveToStorage();
  navigateTo('#/verify-email');
};

// Simulate Email Verify
verifyBtn.onclick = () => {
  const email = localStorage.getItem('unverified_email');
  const acc = db.accounts.find(a => a.email === email);
  if (acc) acc.verified = true;
  saveToStorage();
  alert("Email Verified! Please Login.");
  navigateTo('#/login');
};

// Login
loginForm.onsubmit = e => {
  e.preventDefault();
  const user = db.accounts.find(a => 
    a.email === loginEmail.value && 
    a.password === loginPass.value
  );
  
  if (!user) return alert('Invalid Credentials');
  if (!user.verified) return alert('Please verify your email first.');

  localStorage.setItem('auth_token', user.email);
  setAuthState(true, user);
  navigateTo('#/profile');
};

// Logout
logoutBtn.onclick = () => {
  localStorage.removeItem('auth_token');
  setAuthState(false);
  navigateTo('#/');
};

/* =======================
   UI RENDERERS
======================= */
function renderProfile() {
  profileInfo.innerHTML = `
    <h5 class="card-title">${currentUser.firstName} ${currentUser.lastName}</h5>
    <p class="card-text">
        <strong>Email:</strong> ${currentUser.email}<br>
        <strong>Role:</strong> <span class="badge bg-secondary">${currentUser.role}</span>
    </p>
  `;
}

function renderAccounts() {
  accountsTable.innerHTML = db.accounts.map(a => `
      <tr>
        <td>${a.firstName} ${a.lastName}</td>
        <td>${a.email}</td>
        <td><span class="badge ${a.role === 'admin' ? 'bg-danger' : 'bg-primary'}">${a.role}</span></td>
        <td>${a.verified ? '<span class="text-success">✔ Verified</span>' : '<span class="text-warning">Pending</span>'}</td>
      </tr>`).join('');
}

function renderDepartments() {
  deptList.innerHTML = db.departments
    .map(d => `<li class="list-group-item d-flex justify-content-between align-items-center">
        ${d.name} <span class="badge bg-primary rounded-pill">ID: ${d.id}</span>
        </li>`).join('');
}

function renderRequests() {
  // Logic to show requests only if they exist in DB
}

/* =======================
   INITIALIZATION
======================= */
loadFromStorage();

// Check for existing login session
const token = localStorage.getItem('auth_token');
if (token) {
  const user = db.accounts.find(a => a.email === token);
  if (user) setAuthState(true, user);
}

// Listen for hash changes
window.addEventListener('hashchange', handleRouting);
if (!location.hash) navigateTo('#/');
handleRouting();

</script>
</body>
</html>